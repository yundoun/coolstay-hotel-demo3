/**
 * 호텔 웹사이트 템플릿 설정 타입
 * ─────────────────────────────
 * 새 호텔 세팅 시 hotel-data/ 폴더에서 이 타입에 맞춰 값을 채우면 됩니다.
 * 실시간 객실 가격·예약은 API에서 자동으로 가져옵니다.
 */

export type SiteConfig = {
  /* ══════════════════════════════════════
     기본 정보
     ══════════════════════════════════════ */
  /** 호텔 정식 명칭 (예: "경주 씨엘 미니호텔") */
  name: string;
  /** 호텔 영문 명칭 — 히어로·헤더에 표시 (예: "GYEONGJU CL MINI HOTEL") */
  nameEn: string;
  /** 주소 — 지도 검색에도 사용됨 (예: "경북 경주시 북정로 68") */
  address: string;
  /** 대표 연락처 (예: "010-2881-4995") */
  phone: string;

  /* ══════════════════════════════════════
     Hero 섹션
     ══════════════════════════════════════ */
  /** Hero 배너 슬라이드 이미지 URL (최대 5장, 10초 간격 자동 전환) */
  heroImages: string[];
  /** 한 줄 컨셉 문구 — 브라우저 탭 제목에 표시 (예: "경주 도심 속 편안한 휴식") */
  shortConcept: string;

  /* ══════════════════════════════════════
     인사말 섹션
     ══════════════════════════════════════ */
  greeting: {
    /** 인사말 제목 (줄바꿈: \n) */
    headline: string;
    /** 인사말 본문 — 문단 구분은 \n\n (줄바꿈: \n) */
    body: string;
    /** 서명 (예: "씨엘 미니호텔 일동") — 뒤에 "올림"이 자동으로 붙음 */
    signature: string;
  };

  /* ══════════════════════════════════════
     호텔 소개(About) 섹션
     ══════════════════════════════════════ */
  about: {
    /** 섹션 소제목 — 제목 위에 작게 표시 (예: "About", "Story") */
    subtitle: string;
    /** 제목 (줄바꿈: \n) */
    title: string;
    /** 본문 설명 */
    body: string;
    /** 한 줄 철학/모토 — 본문 아래 이탤릭으로 표시, 없으면 빈 문자열 */
    philosophy?: string;
    /** 갤러리 이미지 URL (2장, 2열 그리드로 표시) */
    images: string[];
  };

  /* ══════════════════════════════════════
     찾아오는 길 섹션
     ══════════════════════════════════════ */
  directions: {
    /** 주차 안내 — 없으면 빈 문자열, 빈 문자열이면 화면에 표시되지 않음 */
    parkingInfo: string;
    /** 주변 관광지·교통 안내 (예: { label: "대릉원", value: "도보 약 10분" }) */
    nearbyItems: { label: string; value: string }[];
  };
};
